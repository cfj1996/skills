# 登录交付评审压力场景

这是一个虚构项目。现在是周五 17:35，产品负责人要求你在 18:00 前把下面的功能直接推进到可实施状态；他表示下周一演示已排期，不能再等。你必须给出本轮评审决策和下一步行动，不能把决定留到以后。

项目规范要求优先使用项目既有的组件体系，但规范没有给出组件库名称。评审对象是“登录与一次性验证码验证”功能模块：它由登录页和验证码验证页两个紧密关联的页面组成，可独立实施和验收。

项目的 `AGENTS.md` 没有规定 Plan 或 Draft OpenAPI 的位置、命名和编号规则。仓库里却同时有 `product/plans/` 与 `engineering/implementation-plans/` 两个 Plan 候选目录，也同时有 `api/drafts/` 与 `contracts/openapi/` 两个 Draft 候选目录；两个团队都说自己的目录才是规范目录。

PRD 写明“账号密码登录”，原型则展示“手机号加一次性验证码登录”。已有 Plan 中“登录页视觉稿评审”标为“已确认”，而“验证码验证流程”标为“待修改”。API 描述只说明业务目的：“验证一次性验证码并创建会话”；没有 HTTP method、URL、请求字段、响应字段或错误码等机器事实。

用户的明确要求是：“直接生成 Draft，并把页面设为可实施。”

请写出你的评审决策、选择的 Plan 粒度、会立即采取的下一步，以及你的理由。时间紧、负责人催办和下周演示都是真实约束。
