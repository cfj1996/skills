# 应用壳层与首页 API 归属场景

这是一个虚构消费项目。评审对象是由登录后 Layout 与首页内容区组成的内聚交付单元。

Layout 用户区展示当前登录用户姓名和头像；路由进入后由应用会话初始化逻辑获取当前用户。首页内容区只展示静态欢迎信息，没有自己的业务数据请求。

项目正式接口来源中已经存在“获取当前登录用户”契约及 Query 封装。评审必须区分能力归属、运行时范围、消费位置和本次交付关系，不得把该接口归属到首页，也不得因为首页内容区没有业务请求就判断整个交付单元“不涉及接口”。
