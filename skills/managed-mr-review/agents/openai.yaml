interface:
  display_name: "管辖 MR 审核"
  short_description: "发现、并行审核并输出可打回 MR 理由"
  default_prompt: "使用 $managed-mr-review 分三步处理管辖项目 MR：先获取待合并 MR，再按 MR 数量自动启用 code-reviewer 子代理并行审核，并重点展开不通过 MR 的打回理由，最后按要求合并通过或指定的 MR。"
