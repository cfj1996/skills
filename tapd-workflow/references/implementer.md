# 阶段三：代码实现

## 目标

在 worktree 中按当前轮次的 `task-plan.md` 执行修改，并生成实现摘要。

## 工作内容

- 切换到正确的 worktree
- 逐项实现修改
- 每次修改后运行相关测试
- 生成当前轮次的 `commit-message.txt`
- 生成当前轮次的 `impl-summary.md`

## 实施要求

- 最小化改动
- 不删除用户已有注释，除非它们错误
- 发现计划失配时，先修正计划再继续
- 不覆盖历史轮次产物

## 输出要求

- 变更文件、测试结果、未完成项都要记录清楚
